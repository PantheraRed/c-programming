#include <stdio.h> // This is a header file which contains the printf function

// This is the main function which is executed when the program is run
int main()
{
    // Prints "Hello World" to the console
    printf("Hello, World!\n");

    // Returns success code 0
    return 0;
}
